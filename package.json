{
  "name": "freestyle",
  "version": "1.0.0",
  "private": true,
  "description": "FreeStyle - Travel website for booking flights, hotels, car rentals, tours and more",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client && npm run lint -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
